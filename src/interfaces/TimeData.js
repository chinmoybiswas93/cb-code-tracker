/**
 * @typedef {Object} DayStats
 * @property {number} totalCodingTime - Total coding time in seconds
 * @property {string} lastSaved - ISO string of last save time
 */

/**
 * @typedef {Object.<string, DayStats>} TimeData
 */

module.exports = {};